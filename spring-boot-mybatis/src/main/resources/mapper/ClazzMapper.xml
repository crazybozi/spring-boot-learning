<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ffz.boot.mybatis.mapper.ClazzMapper">
    <resultMap id="clazzMap" type="com.ffz.boot.mybatis.domain.Clazz">
        <id column="clazz_id" property="clazzId"/>
        <result column="clazz_name" property="clazzName"/>
        <collection property="students" ofType="Student">
            <id column="student_id" property="studentId"/>
            <result column="student_name" property="studentName"/>
            <result column="hometown" property="hometown"/>
        </collection>
    </resultMap>

    <select id="getClazzOneToMany" resultMap="clazzMap" parameterType="int">
        select c.clazz_id, c.clazz_name, s.student_id, s.hometown,s.student_name
        from t_clazz c,
             t_student s
        where c.clazz_id =#{clazzId}
        and c.clazz_id = s.clazz_id
    </select>
</mapper>