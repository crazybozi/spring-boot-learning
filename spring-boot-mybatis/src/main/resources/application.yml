spring:
  datasource:
    url: jdbc:mysql://localhost:3306/spring-boot-learning?useUnicode=true&characterEncoding=UTF-8&useSSL=false&allowMultiQueries=true
    username: root
    password: 28186612
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  #  指定相应接口对应的xml文件所在目录
  mapper-locations: classpath:mapper/*.xml
  #  实体类所在包
  type-aliases-package: com.ffz.boot.mybatis.domain
  configuration:
    # mybatis开启驼峰式命名
    map-underscore-to-camel-case: true
